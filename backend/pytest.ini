[pytest]
# Run all async tests automatically without needing @pytest.mark.asyncio
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Test discovery
testpaths = tests

# Add project root to Python path so `from services.xxx import ...` works
pythonpath = .

# Show short test summary info (-r flags)
addopts = -ra -q
